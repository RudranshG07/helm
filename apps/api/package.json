{
  "name": "@mandate/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --watch src/server.ts",
    "start": "node src/server.ts",
    "test": "vitest run"
  },
  "dependencies": {
    "@mandate/core": "workspace:*",
    "fastify": "^5.2.1",
    "@mandate/db": "workspace:*",
    "@fastify/static": "^8.0.4",
    "@mandate/worker": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^3.0.5"
  }
}
